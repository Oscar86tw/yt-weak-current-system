
document.getElementById('app').innerHTML = layout('驗收單', `
<div class="split"><div class="form-panel"><h2 style="margin-top:0;">驗收單表單 <span class="badge" id="mode">新增</span></h2><input type="hidden" id="docId"><label>客戶搜尋</label><input id="customerSearch" placeholder="輸入客戶名稱關鍵字快速篩選"><label>客戶名稱（下拉選單）</label><select id="customerSelect"></select><div class="row"><div><label>客戶統編</label><input id="customerTaxId" readonly></div><div><label>聯絡人</label><input id="contactPerson"></div></div><div class="row"><div><label>聯絡電話</label><input id="contactPhone"></div><div><label>職位</label><input id="contactJobTitle"></div></div><label>通訊地址</label><textarea id="customerAddress"></textarea><label>完成項目</label><textarea id="acceptanceContent">1. 設備安裝完成。\n2. 線路整理完成。\n3. 系統測試正常。\n4. 基本操作說明完成。</textarea><label>備註</label><textarea id="acceptanceNote">如後續發現非本次施工範圍之既有設備異常，將另行檢測與報價。</textarea><div class="toolbar"><button id="saveBtn">存檔</button><button class="secondary" onclick="window.print()">一鍵匯出 PDF</button><button class="light" id="lineBtn">傳送到 LINE</button><button class="light" id="mailBtn">傳送到 MAIL</button></div></div><div class="preview-panel" id="preview"></div></div>`);
let clients=[], filteredClients=[];
function renderCustomerSelect(list){ customerSelect.innerHTML='<option value="">請選擇客戶</option>'+list.map(c=>`<option value="${c.id}">${c.client_name}</option>`).join(''); if(qs('clientId') && list.some(c=>String(c.id)===qs('clientId'))){ customerSelect.value=qs('clientId'); fillCustomer(); } }
function filterCustomers(){ const k=customerSearch.value.trim().toLowerCase(); filteredClients=clients.filter(c=>(c.client_name||'').toLowerCase().includes(k)||!k); renderCustomerSelect(filteredClients); }
function fillCustomer(){ const c=clients.find(x=>String(x.id)===String(customerSelect.value)); if(!c) return; customerTaxId.value=c.tax_id||''; contactPerson.value=c.contact_person||''; contactPhone.value=c.phone||''; contactJobTitle.value=c.job_title||''; customerAddress.value=c.address||''; renderDoc(); }
function summary(){ const option=customerSelect.options[customerSelect.selectedIndex]; const customerName=option&&option.value?option.text:'未選擇客戶'; return `【${COMPANY.name} 驗收單】\n客戶：${customerName}\n客戶統編：${customerTaxId.value||''}\n聯絡人：${contactPerson.value||''}`; }
function renderDoc(){ const option=customerSelect.options[customerSelect.selectedIndex]; const customerName=option&&option.value?option.text:'＿＿＿＿'; preview.innerHTML=`<div class="doc-head"><div><h2>${COMPANY.name}</h2><div class="doc-sub">施工完成驗收單<br>電話：${COMPANY.phone}<br>地址：${COMPANY.address}</div></div><div class="doc-sub" style="text-align:right;">客戶：${customerName}<br>客戶統編：${customerTaxId.value||'＿＿＿＿＿＿＿＿'}<br>聯絡人：${contactPerson.value||'＿＿＿＿'}<br>電話：${contactPhone.value||'＿＿＿＿'}</div></div><div class="block"><h3>通訊地址</h3><p>${nl2br(customerAddress.value||'＿＿＿＿＿＿＿＿＿＿')}</p></div><div class="block"><h3>驗收項目</h3><ol>${splitLines(acceptanceContent.value).map(x=>`<li>${x.replace(/^\\d+\\./,'').trim()}</li>`).join('')}</ol></div><div class="block"><h3>備註</h3><p>${nl2br(acceptanceNote.value)}</p></div><div class="block"><h3>驗收結論</h3><p>□ 驗收完成　　□ 待改善後複驗</p></div><div class="sign-grid"><div class="sign-box">客戶 / 管理單位簽章：<br><br>日期：＿＿＿＿年＿＿＿＿月＿＿＿＿日</div><div class="sign-box">${COMPANY.name} 現場人員：<br><br>日期：＿＿＿＿年＿＿＿＿月＿＿＿＿日</div></div>`; }
async function saveDoc(){ const payload={ client_id:customerSelect.value||null, contact_person:contactPerson.value||'', contact_phone:contactPhone.value||'', address:customerAddress.value||'', content:acceptanceContent.value||'', note:acceptanceNote.value||'', contact_job_title:contactJobTitle.value||'', customer_tax_id:customerTaxId.value||'' }; if(!payload.client_id){ alert('請先選擇客戶。'); return; } if(docId.value){ await API.updateAcceptance(docId.value,payload); alert('驗收單已更新。'); } else { await API.createAcceptance(payload); alert('驗收單已儲存。'); } }
async function loadEdit(){ const id=qs('id'); if(!id) return; mode.textContent='修改'; docId.value=id; const d=await API.getAcceptance(id); customerSelect.value=d.client_id?String(d.client_id):''; fillCustomer(); acceptanceContent.value=d.content||''; acceptanceNote.value=d.note||''; contactJobTitle.value=d.contact_job_title||''; customerTaxId.value=d.customer_tax_id||customerTaxId.value; renderDoc(); }
async function init(){ clients=await API.getClients(); filteredClients=[...clients]; renderCustomerSelect(filteredClients); ['contactPerson','contactPhone','contactJobTitle','customerTaxId','customerAddress','acceptanceContent','acceptanceNote'].forEach(id=>document.getElementById(id).addEventListener('input',renderDoc)); customerSearch.addEventListener('input',filterCustomers); customerSelect.addEventListener('change',fillCustomer); saveBtn.addEventListener('click',saveDoc); lineBtn.addEventListener('click',()=>shareToLine(summary())); mailBtn.addEventListener('click',()=>shareToMail('驗收單', summary())); await loadEdit(); renderDoc(); }
init().catch(err=>alert(err.message));
